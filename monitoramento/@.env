# ============================================================
# SNMP / ESP32  
# Configurações utilizadas tanto pelo coletor_snmp.py quanto pelo app Flask
# ============================================================

# Endereço IP da ESP32 na rede local
# (ALTERE para o IP real da sua placa ESP32)
ESP32_IP=000.000.000.000

# Comunidade SNMP usada na ESP32
# (ALTERE se tiver configurado outra comunidade)
SNMP_COMMUNITY=public

# Porta padrão do SNMP
SNMP_PORT=161

# OIDs fornecidos pelo agente SNMP da ESP32
# (ALTERE para os OIDs reais configurados no seu firmware)
OID_T1=.1.3.6.1.4.1.99999.1.0
OID_H1=.1.3.6.1.4.1.99999.1.1
OID_T2=.1.3.6.1.4.1.99999.1.2
OID_H2=.1.3.6.1.4.1.99999.1.3


# ============================================================
# MySQL 
# Configurações utilizadas por coletor_snmp.py e app.py
# ============================================================

# Host do banco de dados MySQL
# (ALTERE de “localhost” caso use outro servidor)
MYSQL_HOST=localhost

# Porta do MySQL
MYSQL_PORT=3306

# Nome do banco de dados
# (ALTERE para o nome real criado pelo usuário)
MYSQL_DB=seu_banco

# Usuário do MySQL
MYSQL_USER=seu_usuario

# Senha do MySQL
# (DEFINA sua senha local — não publique senhas reais no GitHub)
MYSQL_PASS=sua_senha

# Intervalo de gravação no banco (em minutos)
# (Tempo entre cada registro salvo pelo coletor)
PERIODO_COLETA_MINUTOS=5


# ============================================================
# ALERTAS  (apenas para coletor_snmp.py)
# ============================================================

# Temperatura limite para disparo de alerta (°C)
ALERTA_LIMITE_C=30.0

# Umidade limite para disparo de alerta (%)
ALERTA_LIMITE_UMID=75.0

# Tempo contínuo acima do limite para gerar alerta (segundos)
MINIMO_SEG_ACIMA=60

# Tempo mínimo para reenviar novos alertas (minutos)
REENVIO_EMAIL_MINUTOS=5

# Frequência de verificação SNMP (segundos)
ALERT_CHECK_SEG=10


# ============================================================
# SMTP – Configurações de e-mail para envio de alertas  
# (apenas coletor_snmp.py)
# ============================================================

# Servidor SMTP (ex.: Gmail, Outlook, etc.)
SMTP_HOST=smtp.seuprovedor.com

# Porta SMTP (465 SSL ou 587 TLS)
SMTP_PORT=465

# E-mail usado para enviar alertas
SMTP_USER=seu_email@example.com

# Senha / App Password
# (NUNCA COLOQUE A SENHA REAL NO GITHUB — coloque apenas no ambiente real)
SMTP_PASS=sua_senha_de_aplicativo

# Para onde os alertas serão enviados
SMTP_TO=email_destino@example.com


# ============================================================
# LOG (usado pelos dois sistemas)
# ============================================================
# Níveis possíveis: DEBUG | INFO | WARNING | ERROR | CRITICAL
LOG_LEVEL=INFO


# ============================================================
# FLASK (apenas app.py)
# ============================================================

# Porta onde a aplicação web será executada
PORT=8000
